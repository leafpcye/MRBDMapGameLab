{
  "name": "mrbd-map-game-lab",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Phase 1A diagnostics plus Phase 1B foreground Location and IMU probes for the Meta Ray-Ban Display web runtime.",
  "scripts": {
    "build-info": "node scripts/generate-build-info.mjs",
    "build": "node scripts/build-site.mjs",
    "predev": "npm run build-info",
    "dev": "node scripts/dev-server.mjs",
    "preview": "npm run build && node scripts/dev-server.mjs --root dist --mount /MRBDMapGameLab/",
    "test": "node --test"
  }
}
